kind: Deployment
apiVersion: apps/v1
metadata:
  name: primeapps-auth
  namespace: primeapps
spec:
  selector:
    matchLabels:
      k8s-app: primeapps-auth
  template:
    spec:
      containers:
        - name: primeapps-auth
          image: primeapps/primeapps-auth:IMAGE_TAG-debian