kind: Deployment
apiVersion: apps/v1
metadata:
  name: primeapps-admin
  namespace: primeapps
spec:
  selector:
    matchLabels:
      k8s-app: primeapps-admin
  template:
    spec:
      containers:
        - name: primeapps-admin
          image: primeapps/primeapps-admin:IMAGE_TAG-debian