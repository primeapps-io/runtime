kind: Deployment
apiVersion: apps/v1
metadata:
  name: primeapps-app-test
  namespace: primeapps
spec:
  selector:
    matchLabels:
      k8s-app: primeapps-app-test
  template:
    spec:
      containers:
        - name: primeapps-app-test
          image: primeapps/primeapps-auth:IMAGE_TAG-debian