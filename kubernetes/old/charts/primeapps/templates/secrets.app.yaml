{{- $root := . }}
{{- range $name, $values := .Values.app.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "fullname" . }}-app-{{ $name | lower }}
data:
  {{- range $key, $value := $values }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
---
{{- end }}
