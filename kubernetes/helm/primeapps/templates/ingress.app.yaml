{{- $secretName := "app-letsencrypt" -}}
{{- if eq .Values.global.tls.type "custom" -}}
  {{- $secretName := .Values.global.tls.secretName -}}
{{- end }}
kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: "{{.Release.Name}}-app-ingress"
  annotations:
    heritage: {{.Release.Service | quote }}
    release: {{.Release.Name | quote }}
    chart: "{{.Chart.Name}}-{{.Chart.Version}}"
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    nginx.ingress.kubernetes.io/server-snippet: |-
      add_header X-Robots-Tag "noindex, nofollow";
    {{- if eq .Values.global.tls.type "letsencrypt" }}
    cert-manager.io/cluster-issuer: letsencrypt-prod
    {{- end }}
    {{- if .Values.global.modsecurity }}
    nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    nginx.ingress.kubernetes.io/enable-owasp-core-rules: "true"
    {{- end }}
spec:
  tls:
    - hosts:
        - "{{.Values.app.domain}}"
      secretName: "{{$secretName}}"
  rules:
    - host: "{{.Values.app.domain}}"
      http:
        paths:
          - path: "/"
            backend:
              serviceName: "{{.Release.Name}}-app-service"
              servicePort: 80