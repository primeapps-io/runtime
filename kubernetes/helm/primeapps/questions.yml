categories:
- Low-Code
questions:
- variable: defaultImage
  default: true
  description: "Use default PrimeApps Docker image"
  type: boolean
  label: Use Default Docker Image
  show_subquestion_if: false
  group: "Container Images"
  subquestions:
  - variable: image.tag
    default: "1.19.337.1-debian"
    description: "PrimeApps Docker image tag. https://hub.docker.com/u/primeapps"
    type: string
    label: Image Tag
  - variable: image.pullPolicy
    default: "IfNotPresent"
    description: "PrimeApps Docker image pull policy"
    type: string
    label: Image Pull Policy
- variable: auth.domain
  default: "auth.yourdomain.com"
  description: "Domain for PrimeApps Auth"
  type: string
  required: true
  label: Auth Domain
  group: "Domain and SSL Settings"
- variable: app.domain
  default: "app.yourdomain.com"
  description: "Domain for PrimeApps App"
  type: string
  required: true
  label: App Domain
  group: "Domain and SSL Settings"
- variable: admin.domain
  default: "admin.yourdomain.com"
  description: "Domain for PrimeApps Admin"
  type: string
  required: true
  label: Admin Domain
  group: "Domain and SSL Settings"  
- variable: global.tls.type
  default: "letsencrypt"
  description: "TLS type for Nginx Ingress"
  type: enum
  required: true
  options:
    - "letsencrypt"
    - "custom"
  label: TLS Type
  group: "Domain and SSL Settings"
- variable: global.tls.secretName
  default: "wildcard-ssl"
  description: "SSL Secret Name"
  type: string
  required: true
  label: Secret Name
  group: "Domain and SSL Settings"
  show_if: "global.tls.type=custom"
- variable: app.smtpHost
  default: "smtp.yourdomain.com"
  description: "SMTP host address"
  type: string
  required: true
  label: SMTP Host
  group: "SMTP Settings"
- variable: app.smtpEnableSsl
  default: "true"
  description: "SMTP enable SSL"
  type: string
  required: true
  label: SMTP Enable SSL
  group: "SMTP Settings"
- variable: app.smtpPort
  default: "587"
  description: "SMTP port"
  type: string
  required: true
  label: SMTP Port
  group: "SMTP Settings"
- variable: app.smtpUser
  default: "smtp-user"
  description: "SMTP username"
  type: string
  required: true
  label: SMTP Username
  group: "SMTP Settings"
- variable: app.smtpPassword
  default: "smtp-password"
  description: "SMTP password"
  type: string
  required: true
  label: SMTP Password
  group: "SMTP Settings"
- variable: postgresoperator.install
  default: true
  description: "Install Postgres-Operator"
  type: boolean
  required: true
  label: Install Postgres-Operator
  group: "Database Settings"
- variable: postgresoperator.serverName
  default: "primeapps-database"
  description: "Database server name"
  type: string
  required: true
  label: Database Server Name
  group: "Database Settings"
- variable: postgresoperator.password
  default: ""
  description: "Database password"
  type: password
  required: true
  label: Password
  group: "Database Settings"
- variable: postgresoperator.storage.backup.class
  default: "standard"
  description: "Storage class for persistent volume - Backup"
  type: string
  required: true
  label: Storage Class - Backup
  group: "Database Settings"
- variable: postgresoperator.storage.primary.class
  default: "standard"
  description: "Storage class for persistent volume - Primary"
  type: string
  required: true
  label: Storage Class - Primary
  group: "Database Settings"
- variable: postgresoperator.storage.secondary.class
  default: "standard"
  description: "Storage class for persistent volume - Secondary"
  type: string
  required: true
  label: Storage Class - Secondary
  group: "Database Settings"
- variable: postgresoperator.storage.backup.size
  default: "20Gi"
  description: "Storage size for persistent volume - Backup"
  type: string
  required: true
  label: Storage Size - Backup
  group: "Database Settings"
- variable: postgresoperator.storage.primary.size
  default: "20Gi"
  description: "Storage size for persistent volume - Primary"
  type: string
  required: true
  label: Storage Size - Primary
  group: "Database Settings"
- variable: postgresoperator.storage.secondary.size
  default: "20Gi"
  description: "Storage size for persistent volume - Secondary"
  type: string
  required: true
  label: Storage Size - Secondary
  group: "Database Settings"
- variable: redis.install
  default: true
  description: "Install Redis"
  type: boolean
  required: true
  label: Install Redis
  group: "Cache Settings"
- variable: redis.password
  default: ""
  description: "Password"
  type: password
  required: true
  label: Password
  group: "Cache Settings"
- variable: redis.global.storageClass
  default: "standard"
  description: "Storage class for persistent volume"
  type: string
  required: true
  label: Storage Class
  group: "Cache Settings"
- variable: redis.master.persistence.size
  default: "20Gi"
  description: "Storage size for persistent volume - Master"
  type: string
  required: true
  label: Storage Size - Master
  group: "Cache Settings"
- variable: redis.slave.persistence.size
  default: "20Gi"
  description: "Storage size for persistent volume - Slave"
  type: string
  required: true
  label: Storage Size - Slave
  group: "Cache Settings"
- variable: minio.install
  default: true
  description: "Install Minio"
  type: boolean
  required: true
  label: Install Minio
  group: "Storage Settings"
- variable: minio.domain
  default: "storage.yourdomain.com"
  description: "Storage Domain"
  type: string
  required: true
  label: Install Minio
  group: "Storage Settings"
- variable: minio.accessKey
  default: "AKIAIOSFODNN7EXAMPLE"
  description: "Access key"
  type: string
  required: true
  label: Access Key
  group: "Storage Settings"
- variable: minio.secretKey
  default: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"
  description: "Secret key"
  type: string
  required: true
  label: Secret Key
  group: "Storage Settings"
- variable: minio.persistence.storageClass
  default: "standard"
  description: "Storage class for persistent volume"
  type: string
  required: true
  label: Class
  group: "Storage Settings"
- variable: minio.persistence.size
  default: "50Gi"
  description: "Storage size for persistent volume"
  type: string
  required: true
  label: Size
  group: "Storage Settings"
- variable: admin.storage.class
  default: "standard"
  description: "Storage class for persistent volume"
  type: string
  required: true
  label: Admin Storage Class
  group: "Other Settings"
- variable: admin.storage.size
  default: "10Gi"
  description: "Storage size for persistent volume"
  type: string
  required: true
  label: Admin Storage Size
  group: "Other Settings"  
- variable: auth.sentryDsn
  default: "https://your:dsn@log.primeapps.io/1"
  description: "Sentry DSN (Auth)"
  type: string
  label: Sentry DSN (Auth)
  group: "Other Settings"
- variable: app.sentryDsn
  default: "https://your:dsn@log.primeapps.io/2"
  description: "Sentry DSN (App)"
  type: string
  label: Sentry DSN (App)
  group: "Other Settings"
- variable: admin.sentryDsn
  default: "https://your:dsn@log.primeapps.io/3"
  description: "Sentry DSN (Admin)"
  type: string
  label: Sentry DSN (Admin)
  group: "Other Settings"  
- variable: app.googleMapsApiKey
  default: "your-google-maps-api-key"
  description: "Google Maps API Key"
  type: string
  label: Google Maps API Key
  group: "Other Settings"