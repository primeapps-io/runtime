{{- if eq .Values.deployment "internal" }}
{{- $root := . }}
{{- range $name, $values := .Values.auth.secrets }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "fullname" . }}-studio-{{ $name | lower }}
data:
  {{- range $key, $value := $values }}
  {{ $key }}: {{ $value }}
  {{- end }}
---
{{- end }}
{{- end -}}