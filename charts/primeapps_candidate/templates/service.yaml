apiVersion: v1
kind: Service
metadata:
  name: {{ template "primeappsapp.fullname" . }}
  labels:
    app: {{ template "primeappsapp.name" . }}
    chart: {{ template "primeappsapp.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "primeappsapp.name" . }}
    release: {{ .Release.Name }}
